import React from "react";
import "./button.css";

const Button = () => {
  return (
    <div>
      <button type="submit" className="text-bold">
        Add to Cart
      </button>
    </div>
  );
};

export default Button;
